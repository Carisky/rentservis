const BASE_URL = "http://localhost:8080";

const api_config = {
    clients: `${BASE_URL}/clients`,
    rents: `${BASE_URL}/rents`,
    landlords: `${BASE_URL}/landlords`,
    apartments: `${BASE_URL}/apartments`,
};

export default api_config;
